# for

Estructura básica de un for en C

```c
for(inicialización; condición; incremento)
{
  sentencia;
}
```

Ejemplo práctico:

```c
for(int i = 0; i < 10; i++)
{
  printf("iteración nro %d", i);
}
```

[Volver](/README.md)
